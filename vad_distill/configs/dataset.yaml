# Dataset configuration
dataset:
  root: "data/raw"  # Path relative to project root, or use absolute path
  manifest_path: "data/manifests/train.jsonl"
  sample_rate: 16000
  manifest_format: "jsonl"  # jsonl or csv

# Manifest format:
# jsonl: {"utt_id": "xxx", "wav_path": "path/to/wav"}
# csv: utt_id,wav_path

